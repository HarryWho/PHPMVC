# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=your_password_here
DB_NAME=adminLTE

# Application Settings
DEBUG=true
BASE_URL=http://localhost
MIN_VERSION=8.0

# Security Settings
# Set HTTPS_ENFORCE=true to redirect all HTTP requests to HTTPS (use in production)
HTTPS_ENFORCE=false
